# Я проектирую своё благополучие

**Telegram-бот** — цифровой наставник для современной молодёжи, который помогает поддерживать баланс трёх составляющих благополучия:  
Физическое (Тело) · Психологическое (Душа) · Социальное (Развитие)

Проект выполнен в рамках открытого межрегионального хакатона **«Код здорового человека»**  
Партнёр трека: Кафедра философии с курсами культурологии, биоэтики и отечественной истории СИБГМУ

## Особенности проекта
- Полное соответствие ТЗ (3 блока, трекеры, SOS, навигатор помощи, дневник настроения, Pomodoro, профориентация)
- Геймификация: очки, ачивки, уровни (за настроение, воду, сон, челленджи)
- Хранение данных пользователя (SQLite)
- Дружелюбный Tone of Voice (на «ты», эмодзи)
- Работает на Python 3.12–3.13 + aiogram 3.13.1
- Готов к Docker + docker-compose

## Стек технологий
- Python 3.12+
- aiogram 3.13.1
- SQLite (встроен в Python)
- python-dotenv
- Docker + docker-compose

## Геймификация
- Очки:
  - +10 за отметку настроения
  - +5 за запись воды
  - +5 за запись сна
  - +50 за выполнение челленджа
- Ачивки:
  - «100 очков» — ⭐ за достижение 100 баллов
  - «Неделя настроения» — 📅 за 7 отметок настроения
  - «Гидратация» — 💧 за 10 записей воды
  - «Сон — сила» — 😴 за 10 записей сна
- Статистика: «Моя статистика» → текущие очки и список ачивок

## Установка и запуск

### Вариант 1 — Локально
```bash
# 1. Клонируем репозиторий
git clone https://github.com/bloodbath88/wellbeing-bot.git
cd wellbeing-bot

# 2. Создаём виртуальное окружение
python -m venv env
env\Scripts\activate        # Windows
# source env/bin/activate    # macOS/Linux

# 3. Устанавливаем зависимости
pip install -r requirements.txt

# 4. Создаём .env файл (скопировать из .env.example и вставить свой токен)
cp .env.example .env
# отредактируй .env → вставь свой BOT_TOKEN от @BotFather

# 5. Инициализируем базу данных
python init_db.py

# 6. Запускаем бота
python main.py

### Вариант 2 — Docker-compose

docker-compose up --build
Структура БД (основные таблицы)

    users(id, username, first_name, registered)

    trackers(id, user_id, date, water, sleep, steps)

    moods(id, user_id, date, mood, note)

    challenges(id, user_id, challenge_name, start_date, current_day, total_days, completed)

    achievements(id, user_id, title, description, emoji, unlocked_at)

    user_points(user_id, points, level)

Примечания

    Переменные окружения:

        BOT_TOKEN — токен бота от @BotFather

    Версии Python 3.12–3.13 поддерживаются.

    Бот использует aiogram 3.13.1 и SQLite — лёгкий деплой без внешних БД.

